# WorkLabs 开发者与智能体工作手册

WorkLabs 是面向高效办公与个人量化工具的免构建单文件 Web 应用仓库。每个子目录均作为一个完全独立、开箱即用的纯前端工具提供。

## 关键约束
- **交付形式**：单 HTML 文件内嵌全部资源，无本地构建，仅用公共 CDN。
- **行情规范**：腾讯 JSONP 节点载入后立删防泄漏；A股量为手/额为万元/51为均价，港股量为股/额为港元/73为均价（港股51为PE、71为收益变动率禁当均价，备用均价由额/量计算）。
- **界面基线**：有数据默认展首项，无数据彻底隐藏，点击行切聚焦不折叠；量化看板人话优先，操作列严防横向溢出。
- **Git规范**：远程已固化鉴权配置，推送直接使用 `git push origin main`，禁止篡改 `.git/config` 中的 remote 与代理参数。
